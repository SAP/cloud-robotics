Kyma uses wildcard domain for the majority of its endpoint, but GCP endpoints do not provide such a functionality.
It might make sense to use it with a custom domain, but we did not evaluate that yet.

